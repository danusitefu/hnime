{
  "name": "nhentai-api",
  "version": "0.0.1",
  "main": "index.js",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "*",
    "express": "*",
    "morgan": "*",
    "pdfkit": "^0.13.0"
  },
  "devDependencies": {
    "@vercel/node": "^1.12.1",
    "nodemon": "^3.0.1"
  }
}
